splash = false

# Path must be absolute: hyprpaper resolves relative paths against its cwd
# (often /), not ~/.config/hypr.
wallpaper {
    monitor =
    path = ~/.config/hypr/wallpapers/brancastle.jpg
    fit_mode = cover
}
